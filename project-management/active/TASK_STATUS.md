# 项目任务状态（TASK_STATUS）

> **文档类型**：Active（全局活态台账）
> **更新频率**：阶段/课程状态切换、里程碑完成、全局断点或下一步变化时
> **维护者**：AI 自动维护
> **读者**：AI 代理（每次启动/接手任务时必读）+ 用户（看全局进度）

## 职责边界（先读）

- **这份文档装什么**：项目**跨课全局**的路线与里程碑、当前课程的**指针级**状态、全局断点与下一步。回答的是"**整个项目到哪了、接下来做什么**"。
- **谁在用**：AI 每次启动 / 接手 / 被问"现在做什么、任务状态"时先读本文件（AGENTS §2、§9-Q1）；用户用它看全局。
- **什么时候写**：阶段或课程状态切换、里程碑完成、全局断点 / 下一步发生变化时立即更新。
- **绝不装什么（边界）**：
  - 单门课的逐讲 / 逐卷 / 批次明细计数、工单勾选、侦查数据、日志、临时过程——一律在该课 `data/_workspace/<course>/`（`tickets/`、`manifest/`、`logs/` 等，**不入库**），本文件只放指向它的一句话指针，**不抄易变计数**（数字以 workspace 台账实时为准）。
  - 问题 / BUG 的生命周期登记在 [ISSUES.md](ISSUES.md)；本文件只在状态行引用其 ID，不重复描述。
  - 稳定、跨课复用的决策与方法在 `docs/project-management/decisions/`（ADR）与 `docs/project-management/memory/`（OKF），不在此展开。
- 易变值（进度数字、当天日期、SHA、剩余量）不固化进本文件，需要时实时读 workspace 台账。

> 最后更新：2026-09-10（名师课6科目同时下载启动：会计/税法/战略/经济法/审计/财管，生产者-消费者模型方案B全部优化完成（多生产者并行+独立缓存+动态轮询+CDP重试）；会计课做题线因账号风控关闭，视频转写已完成）

## 任务总览（全局里程碑）

```
1. 基础设施与流水线        ✅（仓库/飞书/网盘/CDP/视频下载-压缩-转写/做题纯接口，通用能力）
2. 税法课（蔡俊峻）         ✅ finalize（39讲原料 + 116套作业 + 108篇知识详解，本地/网盘/飞书三地同步）
3. 项目管理与文档体系       ✅（ADR-001~018、OKF 工程记忆、SOP/规范/模板体系、I2T 任务工程方法论）
4. 会计课（罗翔）           ⏸️ 暂停（视频转写46/46完成、做题线因风控关闭、知识详解等四来源集齐）
5. 名师课6科目同时下载      🔄 进行中（会计/税法/战略/经济法/审计/财管，生产者-消费者模型方案B）
```

状态标记：✅ 完成 | 🔄 进行中 | ⏳ 待启动 | ⏸️ 暂停 | ⚠️ 有阻塞 | ❌ 取消

---

## 当前阶段：名师课6科目同时下载（ep3平台，profile=`ep3-*-2026`）

> 单课工单拆解、实时计数、侦查与日志以 `data/_workspace/_account/ep3/`（`manifest/`、`logs/`、`keycache/`）与各课 `data/_workspace/<profile>/` 为唯一权威；本节只放跨会话必须先知道的**指针与硬约束**，不抄计数。

| 科目 | profile | 状态 | 指针级说明（细节看 workspace） |
|------|---------|------|-------------------------------|
| **会计** | ep3-accounting-2026 | 🔄 | 510+视频（205G），基础必修66+全面精讲485，双老师（姚远/陈蓓蓓） |
| **税法** | ep3-tax-2026 | 🔄 | 刚开始下载，双老师（杨志国/高蒙） |
| **战略** | ep3-strategy-2026 | 🔄 | 刚开始下载，吴奕主讲（陈岩26考季未挂视频） |
| **经济法** | ep3-econlaw-2026 | 🔄 | 刚开始下载，双老师（沈甜甜/齐萌） |
| **审计** | ep3-audit-2026 | ⏳ | profile已创建，老师信息待补充，视频数量少（16个） |
| **财管** | ep3-finance-2026 | ⏳ | profile已创建，老师信息待补充，视频数量少（23个） |

### 生产者-消费者模型（方案B，已全部优化完成）

- **生产者**：3个并行，每个负责2个科目（会计+税法、战略+经济法、审计+财管）
- **消费者**：4个并行（会计、税法、战略、经济法），等待key后下载
- **独立缓存**：每个科目有独立的key缓存文件（`data/_workspace/_account/ep3/keycache/<profile>.json`）
- **动态轮询**：优先为缓存不足的科目预取，跳过缓存充足的科目
- **CDP重试**：预取失败时最多重试3次，每次间隔5秒
- **每轮预取**：16个key（从8个优化后）
- **消费者等待**：30分钟（重试30次×60秒）

### 硬约束（恢复时第一优先，禁止违反）

- **ep3平台取流**：必须通过CDP hook Worker截获key（AES-128-CBC），禁止静态逆向
- **双老师视频**：按 teacher_id 分拉并按章节路径归并同讲目录，老师进文件名前缀
- **平台字幕**：ep3平台提供VTT字幕，优先下载字幕替代转写（ADR-018）
- **凭证失效（553649434）最后怀疑**：须有只读请求硬证据；排查先怀疑AI自身（端点/参数/文件选取/观测方法）

---

## 已完成课程（履历，明细见 CHANGELOG / ADR / 该课 workspace）

- **税法（26考季·蔡俊峻）✅ finalize**：39 讲视频下载-压缩-转写、18 子目录讲义 OCR、116 套基础作业满分/平台最优 + 6 套冲刺模考；按官方 14 模块组/92 知识点聚合成 108 篇知识详解（14 章 README + 92 知识点 + 2 全局篇），本地/百度网盘/飞书知识库三地同步、元数据（点赞数/学员名）清零。过程件归档于 `data/_workspace/cpa-tax-2026/`（含任务报告）。

---

## 暂停课程（等条件满足后恢复）

- **会计（26考季·罗翔）⏸️ 暂停**：视频转写46/46已完成、讲义OCR已完成；做题线因账号风控10462222关闭（用户拍板不再重刷追分）；知识详解等四来源集齐（题目+用户笔记因风控无法采集）后再Fan-In生成。

---

## 全局下一步（按优先级）

1. **名师课6科目视频下载**（生产者-消费者模型后台续跑）：会计已完成大部分，税法/战略/经济法刚开始，审计/财管待启动；同步推进讲义OCR、字幕下载。
2. **名师课知识来源集齐后**：与用户确认，再Fan-In生成各课知识详解 → 飞书/网盘同步 → 收尾。
3. **会计课恢复条件**：风控解除且用户明确重启做题，二者缺一不可；在此之前禁止自动拉起任何做题脚本。
4. **项目治理**：课程级过程件一律 `data/_workspace/<course>/` 不入库；稳定结论才提炼进 ADR/OKF/SOP。日常开发只 commit 不 push（除非用户要求）。

## 恢复检查清单（异常恢复 / 新会话接续时逐项确认）

- [x] 读本文件 + `docs/project-management/memory/index.md`，确认当前阶段（名师课6科目同时下载）
- [ ] 读 `data/_workspace/_account/ep3/` 下的 manifest 与日志，确认真实进度（不看本文件里的旧数字）
- [ ] 查后台进程：生产者 `round_robin_prefetch`、消费者 `ep3_download_videos`、OCR进程
- [x] 会计课做题线已关闭（2026-09-08 用户拍板）：风控期间禁止自动拉起任何做题脚本
- [ ] 检查各科目独立key缓存数量：`data/_workspace/_account/ep3/keycache/`
