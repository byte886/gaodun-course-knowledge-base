# 规范 Spec · {{功能/任务名}}

> spec（共识成规范）模式产出。记决策与可验收行为，**不写死文件路径、不贴代码**（重构即过期）；易变的进度/计数/日期放实时台账，不抄进本文件。
> 上游：clarify 共识小结。下游：slice 拆工单。落点同共识（单课进 `data/_workspace/<course>/tickets/`）。
> 日期：{{}} ｜ 状态：draft / 已定稿

## 1. 问题是什么
{{现状、痛点、触发背景；用项目自身术语，不写空泛 PRD}}

## 2. 方案是什么
{{选定方向与整体思路，一段话说清；不展开实现细节}}

## 3. 接缝 seam（在哪验证）
- 选定的可独立验证接缝：{{接口很小、背后藏全部逻辑的纯函数/模块/数据边界；优先用已有、层级最高的，理想只留一个}}
- 为什么是它：{{为什么底层被反复改写时，验证不用跟着动}}

## 4. User Stories（每条都要能验收）
- 作为{{角色}}，在{{场景/给定条件}}下，系统应{{可观测行为}}，以便{{价值}}。
  - 验收：给定{{输入/前置}}，期望{{可判定的结果}}。

## 5. 实现决策（已定，含取舍）
- {{决策：……；放弃备选 X，因为……}}

## 6. 验证决策
- 验证哪个接缝、覆盖哪些边界（正常 / 边界 / 异常）；用什么"不同于生成路径"的方式回读。

## 7. 不做什么（Out of scope）
- {{明确排除项，防止执行中扩范围}}

## 8. 落点
- {{本地 md / 飞书 / 其它；同一内容只换存储，不重复维护}}
