# 工单台账 · {{功能/课程/任务名}}

> slice 模式产出的总表，是本任务**唯一实时进度来源**。本地文件模式即工单目录索引；任务系统模式则是 Issue / 依赖的镜像。
> 易变状态在这里**实时更新**，不要把"当前进度/计数"抄进各工单正文（复制即过期）。
> 日期：{{YYYY-MM-DD}} ｜ 上游 spec：{{文件名}}

## 依赖与并行
- 可立即开工（无前置，前沿 frontier）：{{编号}}
- 阻塞关系：{{如 03、04 阻塞 05；05 阻塞 06}}
- 可并行批次：第一批 {{01}} → 第二批 {{03,04 并行}} → ……

## 工单清单（状态：todo / doing / blocked / done / cancelled）
| 编号 | 标题 | 类型 | 前置 | 状态 | 落点/链接 |
|---|---|---|---|---|---|
| 01 | {{}} | refactor | 无 | todo | `01-*.md` |
| 02 | {{}} | feature | 01 | todo | `02-*.md` |

## 拆单说明（发布前三确认的结论）
- 粗细：{{是否有工单被再拆/合并，为什么；一张单不混"改逻辑+批量补数据"}}
- 依赖：{{哪些"只是怕冲突"的伪依赖被去掉，只留真实依赖}}
- 与 spec 差异：{{读真实代码/数据后新增的地基工单}}

## 绞杀类（大范围重构专用，无则删本节）
- [ ] 第一步：加新接口，新旧并存
- [ ] 第二步：分批迁移调用方
- [ ] 第三步：确认无调用后删除旧接口
