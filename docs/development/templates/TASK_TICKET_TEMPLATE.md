# 工单 {{编号如 03}} · {{一句话标题：做成哪件"可见可验收"的事}}

> slice（垂直拆工单）模式产出。一张工单 = 一个垂直切片（穿透数据·逻辑·可见结果，做完即可见可验收），不是"改完一层"。
> 类型：feature（新增能力）/ refactor（不改行为的地基单）/ strangler（加新·迁移·删旧三步）
> 易变进度不抄进本单，实时看 tickets-index 与台账。

## 目标
{{这张单完成后，用户/系统能看到的具体变化}}

## 涉及层（垂直切片应同时穿透）
- 数据/原料：{{需要哪些输入、从哪来、是否已齐}}
- 逻辑/脚本：{{改哪一点行为或新增哪个能力}}
- 可见结果：{{成品/输出/界面哪里能看到；纯后端则写可观测产物与回读方式}}

## 验收标准
- [ ] {{给定输入/前置 → 期望可判定结果（非"效果好/体验好")}}
- [ ] {{用不同于生成路径的方式回读验证}}

## 依赖（只标真实存在的）
- 前置：{{必须先完成的工单编号；无则写"无"}}
- 被谁依赖：{{可选}}

## 不属于本单（边界）
- {{避免把"改逻辑"和"批量补数据"等不同性质的事混进同一张单}}

## 备注
{{读真实代码/数据后发现、spec 未写的必要前提；没有则删}}
